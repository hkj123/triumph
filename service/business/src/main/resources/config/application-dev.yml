#server:
#  servlet:
#    context-path: /ts
spring:
  profiles:
    active: dev
    include: swagger
  datasource:
    url: jdbc:mysql://192.168.3.164:3306/mg_db?useUnicode=true&characterEncoding=utf-8&autoReconnect=true&allowMultiQueries=true&useSSL=false&allowPublicKeyRetrieval=true
    username: mg
    password: MG.1234
    type: com.alibaba.druid.pool.DruidDataSource
  redis:
    host: 192.168.3.164
    password:
    database: 3
    port: 6379
    timeout: 3000s
    jedis:
      pool:
        max-active: 1000
        max-wait: 60000
        max-idle: 400
        testOnBorrow: true
        testOnReturn: true
eureka:
  instance:
    leaseRenewalIntervalInSeconds: 10
    metadata-map:
      management:
        context-path: /actuator
    health-check-url-path: /actuator/health
    prefer-ip-address: true
    ip-address: localhost
  client:
    registryFetchIntervalSeconds: 5
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/

